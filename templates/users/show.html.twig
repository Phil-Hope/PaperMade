{% include 'include/header.html.twig' %}



<div class="row">
    <div class="col-2">
        {% include 'include/adminpanel.html.twig' %}
    </div>
    <div class="col-10">
    <h3 class="text-center">User Details</h3>

    <table class="table">
        <tbody>
            <tr>
                <th>User ID:</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email:</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Roles:</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>FirstName:</th>
                <td>{{ user.FirstName }}</td>
            </tr>
            <tr>
                <th>LastName:</th>
                <td>{{ user.LastName }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn-sm btn-primary btn" href="{{ path('users_index') }}">Back</a>
    <a class="btn-warning btn-sm btn" href="{{ path('users_edit', {'id': user.id}) }}">Edit</a>

    {{ include('users/_delete_form.html.twig') }}
    </div>
</div>